const functionAdc = (number1, number2) => {
    return number1 + number2;
  };
const functionSub = (number1, number2) => {
    return number1 - number2;
  };

module.exports = {functionAdc,functionSub }
